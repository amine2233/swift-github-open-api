name: Print Environment Variables

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  print-env:
    runs-on: ubuntu-latest
    steps:
      - name: Print all environment variables
        run: env | sort
